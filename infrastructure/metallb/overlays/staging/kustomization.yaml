apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Append local network settings onto the base installation
images:
  - name: staging-configs
resources:
  - ip-pool-patch.yaml
